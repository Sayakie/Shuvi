import { BaseClient } from 'discord.js'
import type { Client } from '../Client'
import type { GiveawayOptions } from '../types'

class Giveaway extends BaseClient {
  client: Client
  manager: GiveawayManager

  constructor({ client, manager, options }: GiveawayOptions) {
    super()

    this.client = client
    this.manager = manager
  }
}
